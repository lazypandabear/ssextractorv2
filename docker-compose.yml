services:
  smartsheet-app:
    build:
      context: .
    image: smartsheet-extractor-app:latest
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - /app/tempData:/app/tempData
    command: ["python", "app/app.py"]
    restart: unless-stopped
